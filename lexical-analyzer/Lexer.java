import java.util.List; 
import java.util.ArrayList;

public class Lexer{
    public List<String> tokens = new ArrayList<>();

    public static void lex(String code){
        
        int i = 0;
        while (i <= code.length()){
            // read numbers (machine / regular language)
            // tokens.add(instance_of_Token.toString())
            
            // read identifiers (machine / regular language) 

            // read   
        }
    }
} 